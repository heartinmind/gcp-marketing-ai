# Terraform 변수 설정 예제
# 실제 사용 시 terraform.tfvars로 복사하여 값을 설정하세요

# GCP 프로젝트 ID (필수)
project_id = "your-gcp-project-id"

# 기본 리전
region = "us-central1"

# 환경 설정 (dev, staging, prod)
environment = "dev" 